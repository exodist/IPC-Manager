package IPC::Manager::Coordinator;
use strict;
use warnings;

use Object::HashBase qw{
    <name
    <callback
    <interval
};

sub run {
    my $self = shift;

    # Watch for writes to our handle
    # Watch for 
    #

    while (1) {
        my @handles = 
    }
}

1;
