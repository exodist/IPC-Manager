package IPC::Manager::Coordinator;
use strict;
use warnings;

use parent 'IPC::Manager::Coordinator::This';
use Object::HashBase qw{
    <xxx
};

sub run {
    my $self = shift;

    # Watch for writes to our handle
    # Watch for 
    #

    while (1) {
        my @handles = 
    }
}

1;
